# Legacy experimental scripts

This directory preserves archival snapshots of the two files that were present in the repository
before it was converted into a Julia package:

- `NMFfirst.jl`
- `unittest.jl`

They are archived for provenance only. They are not loaded by
BilinearProjections.jl and are not part of the package test suite.
